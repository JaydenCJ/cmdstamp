// cmdstamp — run declared commands and stamp their output into marked
// Markdown regions, with a verify mode that fails when docs go stale.
//
// version:    0.1.0
// author:     JaydenCJ
// license:    MIT
// repository: https://github.com/JaydenCJ/cmdstamp
// keywords:   markdown, documentation, docs-drift, cli, automation, verify, developer-tools
//
// Zero runtime dependencies: the require list below is intentionally empty
// and must stay that way (see CONTRIBUTING.md, "Ground rules").
module github.com/JaydenCJ/cmdstamp

go 1.22
